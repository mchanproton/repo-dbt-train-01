version: 2

sources:
  - name: salesforce_raw
    database: dbt_cloud_catalog
    schema: dbt_schema
    config:
      freshness:
        warn_after:
          count: 48
          period: hour
        error_after:
          count: 72
          period: hour
      loaded_at_field: _loaded_at
    tables:
      - name: t1_sfdc_oppty